import Paper from '@mui/material/Paper';
import { styled } from '@mui/material/styles';

export const ShoutBoxPaper = styled(Paper)(({ theme }) => ({
    backgroundColor: '#edf0f5',
    height: '100%',
    display: 'flex',
    padding: '5px'
}))